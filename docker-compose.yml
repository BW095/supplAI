version: "3.9"

services:
  supplai:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./state:/app/state
      - ./data:/app/data
      - ./models:/app/models
    env_file:
      - .env
    restart: unless-stopped
